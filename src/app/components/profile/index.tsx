const Profile = () => {
    // unused variable to maintain func body
    const foo = 'bar';
    return (
        <div>
            <a href="/profile">Profile</a>
        </div>
    );
};

export default Profile;
